# TODO — D-Lab

## Features

- [ ] Simplificación multimedia a Jellyfin+qBittorrent+aMule (2026-08-29): retirados Sonarr/Radarr/Prowlarr/FlareSolverr/Jellyseerr/es-badge; añadidos `amule.yaml` (ngosang/amule, 4711+4662/4672/4665), `ingress/certificate-{qbittorrent,amule}.yaml` (`torrent.elarreglador.eu`/`amule.elarreglador.eu` con TLS), `networkpolicy-multimedia.yaml`, `storage.yaml` podado (solo `qbittorrent/jellyfin/amule-config`), `init-media-dirs` con `/data/amule`, `amule-expose-p2p.sh`; liberadas IPs .56/.57. Pendiente: exponer P2P en DV0 y probar login/WebUI + ingest en Jellyfin
- [x] Landing: sección «Acerca de ti» (live-fingerprinting del visitante) — `files/landing/acerca-de-ti.{js,css}`
- [x] Landing: rediseño ciberpunk/retrofuturista (paleta cian/magenta/marino/negro, tipografías auto-alojadas Orbitron/Exo 2/Share Tech Mono, rejilla + scanlines + glow) — `styles.css`, fuentes `.woff2` en `files/landing/`
- [x] Landing: cambio de estética a **dieselpunk** (latón/cobre/cuero/pergamino, Oswald + Roboto Slab + Special Elite, remaches y filetes de latón); el conmutador dark/light y su comportamiento se mantienen intactos
- [x] Landing: cambio de estética a **consola de operaciones** (minimal y profesional: azul marino + cian turquesa + rosa magenta, Space Grotesk + Inter + IBM Plex Mono, sin rejillas/scanlines/remaches); cache-busting `?v=YYYYMMDD` en `index.html` (nginx no envía `Cache-Control`); el conmutador dark/light y su comportamiento se mantienen intactos
- [x] Landing: retoques de estilo (estética GitHub): se elimina la barra bajo los títulos de sección y se normaliza el ritmo vertical (mismo hueco entre bloques: secciones 5rem, bloques hermanos internos 2.5rem, título→párrafo 1.4rem)
- [x] Landing: botones de contacto movidos a la barra inferior (footer como «barra» con fondo `--surface`); se elimina la sección «Contacto» y su enlace del menú; barras superior e inferior con `--border-bar: #7c8094` (mismo gris azulado en claro y oscuro)
- [x] Landing: barras mucho más oscuras — `--bar-bg: #161b22` en ambos modos (claro y oscuro), con `--bar-fg` (texto claro sobre la barra), `--bar-accent` (enlaces/hover azul claro) y botón de tema transparente sobre la barra; se mantiene `--border-bar: #7c8094`
- [x] Landing: cambio de estética a **«GitHub» (Primer)** — plana/neutra, paleta GitHub dark/light (azul `#2f81f7`/`#0969da`, verde `#3fb950`/`#1a7f37`), **tipografías de sistema** (se eliminan las fuentes auto-alojadas; ConfigMap 253→134 KB); el conmutador dark/light y su comportamiento se mantienen intactos
- [x] Landing: tema por defecto del sistema con conmutador manual claro/oscuro (persistencia en `localStorage` solo al pulsar el botón, `data-theme` + `meta color-scheme`, script anti-flash en `<head>`, fallback sin JS vía media query) — `index.html`, `styles.css`
- [x] `deploy-landing.sh`: guard por bandas frente al techo de etcd (~1,5 MiB) en lugar del aborto rígido de 1 MiB
- [x] Landing: tarjeta **«IA local (Ollama)»** en la sección «Servicios D-Lab» (2026-08-20): `dev.svg`, LLM autoalojados de uso interno (sin acceso desde el exterior); ConfigMap ~182 KB base64, pods Running, curl 200 y tarjeta presente en `elarreglador.eu`. Documentado en README-TECH.md § Web pública
- [ ] Vigilar tamaño de `acerca-de-ti.js`: si supera ~1 MiB raw, migrar la landing a imagen nginx custom (Dockerfile + import en nodos, ver README-TECH § Web pública)
- [x] telegram_bot: notificaciones Telegram en `pods/telegram-bot` (ClusterIP 8080, `/notify` + `/alert`, Opción A python:alpine + ConfigMap, acceso abierto intra-cluster, Secret gitignored) — `files/telegram-bot/telegram-bot.yaml` (ConfigMap + Deployment python:3.12-alpine + Service), `files/telegram-bot/networkpolicy.yaml` (ingress abierto + egress 443), `scripts/deploy-telegram-bot.sh` (Secret por stdin). Verificado 2026-08-22: `getUpdates`→@Arreglador/`getMe` @Dlab_mrbot/`sendMessage` OK, `info_sensible/telegram.env` (600, gitignored, offset limpiado), `rollout` OK `10.244.1.109 k8s-worker-2`, `ClusterIP 10.111.44.193:8080`, `/health` 200, `/notify` `message_id 5` y `/alert` `message_id 6` desde pods/ia. Repo sin histórico (sin polling/PVC/log de `text`). Documentado en `03-Aplicaciones.md`
- [x] Cluster-AI Fase 14 (A — esqueleto polling pong + whitelist get nodes) (verificado 2026-08-22): bot separado `@Dlab_assistant_bot` (`TELEGRAM_AI_TOKEN` 8869… + `TELEGRAM_CHAT_ID` 836571451 allowlist), `files/cluster-ai/{api,rbac,networkpolicy,whitelist,docs-sync}.yaml`, `scripts/deploy-cluster-ai.sh` idempotente (Secret por stdin vía `ssh $KUBECTL_HOST`), NetworkPolicy egress 53+443/6443→10.96.0.0/12+192.168.1.0/24+0.0.0.0/0 +11434+8080, RBAC RO `get/list/watch` sin secrets (`auth can-i get secrets → no`), Deployment 1 réplica Recreate 100m/128Mi→300m/512Mi `readOnlyRootFilesystem/runAsNonRoot`, `/health {"ok":true}`, polling 3s `asyncio.to_thread` (no bloquea liveness), `/get nodes → 4 Ready` + `/help`+`pong`, whitelist auditable + rate 5/min + truncado 4096, `telegram-bot:8080/notify → message_id`
- [x] Cluster-AI Fase 14 (B — IA + live K8s) (verificado 2026-08-22): orquestador determinista `regex→tool` sin / → Ollama (`OLLAMA_URL=http://ollama.ia.svc:11434` `MODEL=qwen2.5-coder:3b` `/api/generate` fallback `/api/chat` 30s), live K8s `fetch_cluster_data` (nodes/pods/deployments), `/status` con `build_prompt` + Ollama, `/get ns|pods|deployments|events`, `/logs --tail 100 --since 10m` + `/top nodes|pods` (metrics 404 graceful) + `/alerts` + `/help`, `sin /` Ollama truncado 4096, `asyncio.to_thread` no bloquea liveness, verificado `hola→✅Hola` y `cmd_status 1094 chars` + `fetch 4 Ready` + `cmd_ns/pods/deps`
- [x] Cluster-AI Fase 14 (C — RAG docs) (verificado 2026-08-22): CronJob docs-sync 15m `git clone --depth 1 D-Lab → /cache/docs` PVC docs-cache 1Gi nfs-storage + `POST /reindex → 183 chunks` `all-MiniLM-L6-v2` 384 + Chroma `TOP_K=3` (in-memory `Client` + `HF_HOME=/tmp/hf_cache` + `readOnlyRootFilesystem` + `2Gi` + `build-essential`), `/docs Gluster → README-TECH.md:2597` (Gluster) + `/docs hardware → Hardware.md:1` (Dell) + `sin /` VIP `192.168.1.30` → `03-Aplicaciones.md:28` `README-TECH.md:41`, `timeout 90s` + `retry 1` + `Fuentes:` condicional (solo con respuesta e info de fuente), `simulate-cluster-ai.sh` Plan B CLI (`--all` 12 cmds + rate 5/min) + `POST /simulate` + `GET /debug/rag`, `normalize ¿?` + `TOP_K=3` + `VIP keyword fallback`
- [x] Cluster-AI Fase 14 (D — Monitor + whitelist completa + Alertmanager) (verificado 2026-08-22): `monitor.py` 5m `APScheduler` (pods `CrashLoop` only, `Pending >5m`, `nodes NotReady/DiskPressure/MemoryPressure`, `storage VIP 192.168.1.30:2049` socket, `etcd readyZ` `https://10.96.0.1:443/readyz`, `cert-manager <30d` via `CustomObjects` + `telegram-bot:8080/notify` + `Alertmanager` `webhook http://telegram-bot.pods.svc:8080/alert` (Secret `alertmanager.yaml` patched `receiver telegram` + `pod delete` restart), `NetworkPolicy` `2049/111` + `whitelist` `10s/3000c/5/min audit`, verificado `run_checks []` healthy + `curl /alert → message_id 23`
- [x] Cluster-AI Fase 14 (E — Hardening + docs) (verificado 2026-08-22): `readOnlyRootFilesystem/runAsNonRoot` `runAsUser 1000` `drop ALL` `seccomp RuntimeDefault`, `auth can-i get secrets → no` (RO `get/list/watch` only), `Quota ia` `requests 720m/4` `limits 4100m/8` `pods 3/3` `pvc 2/2`, `linter yaml ok` + `whitelist 13/13` + `RAG 16/16` + `simulate --all` (12 cmds + VIP `192.168.1.30` HTML), `03-Aplicaciones.md` Cluster AI section + `README-TECH.md` Fase 14 (polling 3s + monitor 5m + RAG 183 + Ollama 90s retry), `git diff --stat` 6 files sin secretos, `NetworkPolicy` `80/443/6443/2049` `allowlist` `10s/3000c/5/min audit`

## Fix

- [x] `signals.hw.netType.value` → `signals['hw.netType'].value` (acceso por clave punteada a un mapa de ids planos)
- [x] `recall()` colgada si un almacén (IndexedDB/Cache) no responde → guarda `Promise.race` con respaldo de primera visita
- [x] Versión de navegador limpia (sin client hints ruidosos "Not=A?Brand...")

## Tareas finalizadas

- [x] Simplificación multimedia a Jellyfin+qBittorrent+aMule (2026-08-29): retirada cadena Jellyseerr→*arr→Prowlarr→FlareSolverr+es-badge; stack final `files/multimedia/{jellyfin,qbittorrent,amule}.yaml` + `ingress/certificate-{qbittorrent,amule}.yaml` (`torrent.elarreglador.eu`/`amule.elarreglador.eu` TLS + login app), `networkpolicy-multimedia.yaml` (media-public+qbittorrent+amule+egress), `storage.yaml` solo `qbittorrent/jellyfin/amule-config` (NFS, móviles), `_retirado/` conserva `sonarr/radarr/prowlarr/flaresolverr/jellyseerr/es-badge` y `scripts/_retirado/` el wizard/language/verify. `init-media-dirs` con `/data/amule`. IPs liberadas .56/.57 ( .54/.55 reasignadas a amule). `deploy-multimedia.sh` simplificado + `amule-expose-p2p.sh` + `backup-multimedia.sh` para 3 apps. Docs `03-Aplicaciones.md` reescrita. Pendiente despliegue real (`./scripts/deploy-multimedia.sh` + `amule-expose-p2p.sh`) y verificación WebUI
- [x] Migración multimedia a NFS + masters blindados (2026-08-20): las configs de qBittorrent, Sonarr, Radarr, Prowlarr, Jellyfin y Jellyseerr pasaron de los PV `local-static` a los PVC NFS `*-config` (jobs `migrate-*`); los `nodeSelector kubernetes.io/hostname` → `eu.elarreglador/worker=true` y es-badge perdió el suyo. `k8s-master-1` recibió label `control-plane` + taint `NoSchedule`; se desalojaron landing, es-badge, Prometheus y qBittorrent del master. **Failover bidireccional real verificado** (qB worker-1→2, radarr worker-2→1). **Reversión selectiva (mismo día)**: sonarr reprodujo el crash-loop histórico de SQLite sobre NFS durante RSS sync; diagnóstico a fondo → fsync ~66 ms en el volumen Gluster **Replicate 1×2 sobre HDD** (`vol-storage`, bricks `.31/.32:/mnt/data/brick`; disco local ~2,5 ms); `local_lock=all` **ignorado en NFSv4** e inútil en NFSv3 (el cuello es el fsync I/O, no los locks). SQLite sobre este NFS **inviable para los \*arr** → sonarr/radarr/prowlarr revertidos a `local-static` (config local, anclados; `/ping` 1-6 ms, RSS sync instantáneo, 0 reinicios). qBittorrent/Jellyfin/Jellyseerr permanecen en NFS con movilidad (0 reinicios, en monitorización). `storage.yaml` restaurado (13 PVC + 6 PV) con nota de estado. Documentado en 03-Aplicaciones.md y README-TECH.md. **Superseded el 2026-08-29** por la simplificación (cadena retirada).

- [x] opencode en G9: registrados los modelos de Ollama del cluster con prefijo `D-Lab_` en `~/.config/opencode/opencode.jsonc` (2026-08-20): providers `ollama-lan` y `ollama-wg`, claves `D-Lab_<modelo>` con `id` = nombre real en Ollama y `limit.context` verificado en el pod (`/api/tags`). **1ª fase** (6 modelos): se añadieron los 6 del PVC y se detectó el bug de opencode 1.18.19 que ignora `tool_call: false` (#35432) → workaround agente `d-lab-chat`. **2ª fase** (reemplazo por modelos con tools): `deepseek-coder:1.3b` + `yi-coder:1.5b` → **`qwen2.5-coder:1.5b`** (32768); `granite-code:3b` → **`granite3.2:2b`** (131072); eliminados los 3 sin tools del PVC (ollama rm) y el agente `d-lab-chat`. Catálogo final (5, todos declaran tools en el manifiesto): granite3.2:2b, llama3.2:3b, qwen2.5-coder:1.5b, qwen2.5-coder:3b, smollm2:1.7b. **Hallazgo clave**: ninguno de estos ≤3B emite `tool_calls` nativos fiables en opencode (responden con JSON en texto que no se ejecuta; caso #234) → el agente `build` vale para conversación/código pero no para tareas que exigen herramientas. **Gotchas**: OOM del pod (4 GiB) al cargar varios modelos en la ventana keep_alive (confirmado OOMKilled); granite3.2:2b es "thinking" y lento (1-2 min en frío); granite3.1-dense:2b descartado (error de servidor persistente). Verificado con `opencode run` vía WG. Documentado en 03-Aplicaciones.md

- [x] LLM local (Ollama + qwen2.5-coder:3b) en el cluster (2026-08-18): namespace `ia`, Deployment `ollama` (`ollama/ollama:0.32.14`, pin fijo) **solo en los workers** (`nodeSelector eu.elarreglador/worker=true`), PVC `ollama-models` 20Gi `nfs-storage`, Service NodePort `31434`, NetworkPolicy `ollama-allow` (ingress 11434 abierto + egress DNS y TCP/443 a rangos Cloudflare — `registry.ollama.ai` + R2, **gotcha**: sin esa regla el pull muere en `dial tcp 172.64.x.x:443: i/o timeout`), ResourceQuota + LimitRange, hardening (runAsNonRoot, readOnlyRootFilesystem, drop ALL). `scripts/deploy-ollama.sh` etiqueta workers y despliega. Accesos verificados: LAN `192.168.1.31/.32:31434` (200) y WG `10.8.0.11:31434` vía LXC proxy device `proxyollama` en D1 (200); `/v1/chat/completions` responde. WireGuard G9: peer nuevo `10.8.0.100` en DV0 (PSK compartida), config `files/wg0-client-g9.conf` (split-tunnel sin DNS). **Fix hub-and-spoke**: D1/D2 `AllowedIPs` cliente → `10.8.0.0/24, fd42:42:42::/64` + ruta manual `10.8.0.0/24 dev wg0` (`wg syncconf` no toca rutas). opencode en G9: providers `ollama-lan` (`192.168.1.31:31434/v1`) y `ollama-wg` (`10.8.0.11:31434/v1`) con `@ai-sdk/openai-compatible`, modelo `qwen2.5-coder:3b` (contexto 32768); verificados ambos con `opencode run`. Documentado en 03-Aplicaciones.md y 01-Network.md
- [x] Benchmark de modelos ≤3B para el LLM local y elección definitiva (2026-08-18): probados 8 modelos en el pod real (2 CPU/3 Gi, i3-7100T) con 2 consultas por modelo (fría descartada + **caliente medida**), prompt «Responde con una sola palabra: pong», TTFT y total vía pod helper curl. Tabla: tinyllama:1.1b 0.53 s, qwen2.5-coder:1.5b 0.59 s, qwen2.5:1.5b 0.62 s, qwen2.5-coder:3b 1.01 s (elegido), gemma2:2b 1.22 s, llama3.2:3b 6.19 s (supera 5 s). **qwen3:4b y phi3:mini descartados**: al cargarlos (2.2-2.5 GB) el daemon no responde a tiempo y el **liveness probe (timeout 1 s)** reinicia el pod. Modelo definitivo: `qwen2.5-coder:3b` (restaurado tras el `ollama rm` de la limpieza). Tabla documentada en 03-Aplicaciones.md
- [x] Benchmark de tarea de programación (evaluador aritmético, 6 lenguajes) (2026-08-19): 6 modelos (qwen2.5-coder:3b, granite-code:3b, llama3.2:3b, yi-coder:1.5b, deepseek-coder:1.3b, smollm2:1.7b) × Dart + C + Python + bash + JS + Java = **1 solo acierto de 36**: **yi-coder:1.5b en JS (10/10 en el corpus), pero con `eval()` → RCE** (CiberSeguridad 1/10). Ningún otro modelo+lenguaje pasó la validación; fallos dominantes: código fragmentado, APIs inventadas y precedencia/división entera mal resueltas. Durante la ronda se subió el límite de memoria del pod a **4Gi** y el **timeout de las probes a 5 s** (con 3Gi la carga de modelos grandes mataba el pod). Resultados, prompts y corpus documentados en **`Benchmark-LLM.md`** (creado 2026-08-19) y tabla resumen en 03-Aplicaciones.md. **Los 6 modelos se conservan descargados en el PVC** (decisión 2026-08-19, restaurados tras la limpieza).

- [x] Monitorización de DV0 (jumpbox/VPN/nginx) en Prometheus + landing (2026-08-18): node_exporter v1.12.1 instalado en DV0 (unit systemd `node-exporter`, bind `10.8.0.1:9100`, solo túnel WG, sin firewall); relay socat en D2 `node-exporter-relay-dv0` (`192.168.1.12:19200 → 10.8.0.1:9100`, patrón de `node-exporter-relay-d1`); Service + Endpoints + ServiceMonitor `dv0-host` en `files/monitoring/dv0-host.yaml` (relabel `host="dv0"`). Dashboard «Sistema D-Lab» ampliado a 7 tarjetas: panel «DV0 · Host» a ancho completo (w=24) en la 1.ª fila (1+3+3), D1 y D2 reubicados a filas 2 y 3. Verificado: target `serviceMonitor/monitoring/dv0-host/0` up, `node_memory_MemTotal_bytes{host="dv0"}` = 394 MB, RAM uso 65.7%, dashboard v3 (7 paneles) por API, landing y public dashboard 200. Documentado en 03-Aplicaciones.md y README-TECH.md
- [x] Landing: sección «Monitorización en vivo» con gráficas CPU/RAM de D1, D2 y los 4 nodos K8s (2026-08-18): dashboard provisionado «Sistema D-Lab» (uid `sistema-dlab`, `files/monitoring/grafana-dashboard-sistema-dlab.yaml`) con 6 tarjetas stat (hosts D1/D2 por label `host` del job `host-node` — `files/monitoring/host-node.yaml`, versionado — y nodos por `instance`); expuesto sin login vía **public dashboard** + `<iframe>` en la landing (tema fijo oscuro, ventana 24 h). En grafana.ini: `allow_embedding = true` + feature toggle `publicDashboards`. `scripts/ensure-public-dashboard.sh` (idempotente) garantiza el registro efímero y guarda la URL en `info_sensible/public-dashboard.env` (gitignored). `deploy-landing.sh` resuelve el placeholder `__PUBLIC_DASHBOARD_URL__` en staging. **Gotcha**: en Grafana 13 la ruta pública es `/public-dashboards/<token>` (con guion); `/public/dashboards/` responde 302 a `/login`. Verificado: 6/6 máquinas con métricas, web 200, iframe inyectado y public dashboard 200 desde internet. Documentado en 03-Aplicaciones.md y README-TECH.md
- [x] Indexadores torrent multilingüe con contenido en castellano (2026-08-17): sustituidas las fuentes anglocéntricas (The Pirate Bay, YTS, EZTV, Nyaa.si) por 1337x, Torrent9, LimeTorrents y Torrent Downloads en Prowlarr. 1337x vía FlareSolverr (tag cloudflare, `requestTimeout=120`); Torrent9/LimeTorrents/Torrent Downloads directos (a Torrent9 se le quitó el tag cloudflare, devolvía 0 resultados con proxy). Sync fullSync a Sonarr/Radarr verificado; búsqueda Prowlarr con releases `DUAL SPA-ENG`/`MULTI`/`LATINO` en las 4 fuentes. `multimedia-verify.sh` 32/32. Wizard actualizado en scripts/multimedia-wizard.sh. Documentado en 03-Aplicaciones.md
- [x] Interfaz de Jellyseerr en castellano (2026-08-17): locale `es` en `POST /api/v1/settings/main` (por defecto) y en `POST /api/v1/user/1/settings/main` (admin, preservando permisos=2). Verificado en `settings/public` y `GET /user/1`. Documentado en 03-Aplicaciones.md
- [x] Política de idioma de descargas (castellano + fallback inglés) en Sonarr/Radarr (2026-08-17): `scripts/multimedia-language.sh` crea/actualiza 5 CFs — "Español (Audio)" (110), "Audio dual" (110), "VOSE" (100), "Inglés (con subs)" (50), "Inglés (sin subs)" (1) — y puntúa el perfil "Any" con `minFormatScore=1`. Jerarquía: doblado+dual+subs 320 > dual 220 > doblado 110 > VOSE 100 > Inglés+subs 51 > Inglés solo 1 > sin match 0. Validado con `/api/v3/parse` (`ENG.Subs` → 151, `ENG` → 1). Documentado en 03-Aplicaciones.md
- [x] The Punisher visible en Jellyfin (2026-08-17): serie ya descargada completa (26/26 eps, 22 GB); fix permisos NFS (`chown -R 1000:1000 /data/media`) + escaneo completo librería TV Shows (`/Library/Refresh`). Verificado 26 episodios con MediaSources vinculados.
- [x] Insignias de idioma en Jellyseerr (es-badge) (2026-08-17): inyección client-side vía ingress-nginx (`sub_filter` + `configuration-snippet`) del script `files/multimedia/es-badge.js`, servido por el Deployment `es-badge` (nginx:alpine) que también proxya `/jf-api/` → Jellyfin con token server-side (Secret `es-badge-jf`, sin fugarlo al navegador). Insignias ES/LAT/SUB/EN según los MediaStreams de Jellyfin (series agregan episodios). ConfigMap ingress-nginx: `allow-snippet-annotations: true` + `annotations-risk-level: Critical`; controller con `--enable-annotation-validation=false` (requisito v1.12+). NetworkPolicy `es-badge-ingress` para que el ingress llegue al :80 del pod. Lógica validada por unidad (10/10 clasificación + 4/4 agregación de series); verify 32/32; inyección y `/jf-api/` verificados por curl (200, HTML con `<script src="/es-badge.js">`). Limitación: librería de Jellyfin vacía → sin insignias visibles hasta tener contenido. Documentado en 03-Aplicaciones.md
- [x] Insignias de idioma en Jellyseerr ANTES de la descarga (es-badge v2) (2026-08-17): el Deployment `es-badge` pasa a 2 contenedores — nginx (JS + proxy `/es-badge-api/` → `127.0.0.1:8080` y `/jf-api/` conservado) y **api** (node:20-alpine, `files/multimedia/es-badge-api.js`). El backend consulta Prowlarr por keyword en los 3 indexadores directos (Torrent9, LimeTorrents, Torrent Downloads; `indexerIds=6&7&8`), filtra por coincidencia de tokens del título, clasifica los releases (ES/LAT/SUB/EN) y cachea en emptyDir (TTL 24 h) con semáforo de 1 búsqueda concurrente. El JS cliente encola tarjetas visibles (cola FIFO + MutationObserver + scroll debounced). Verificado E2E: Dune Prophecy → `{"ES":true,"LAT":true,"SUB":true,"EN":true,"total":106}`, 2ª llamada `cached:true`, sin fuga de `X-Api-Key`; tests del clasificador (10), filtrado de título, backend (caché) y JS cliente (3) en verde; verify 32/32. Documentado en 03-Aplicaciones.md
- [x] Fix es-badge v2: extracción de tarjetas sin overlay hover (2026-08-17): la portada se renderiza en cliente (sin SSR) y el `href`/`title-card-title` solo existen al hacer hover (React los monta), por lo que la v2 no pintaba insignias ni emitía peticiones. El JS cliente ahora intercepta `window.fetch`, captura las respuestas JSON de `/api/v1/discover` y `/api/v1/search` y construye un mapa `posterPath → {tmdbId, mediaType, title}` resolviendo cada tarjeta por la URL de su `<img>` (póster TMDB), con el overlay como fallback y dedupe por `mediaType:tmdbId`. Modo debug `?es-debug=1` (logs + `window.__esBadge`). Tests de unidad del nuevo parseo (18/18) en verde; ConfigMap `es-badge-js` aplicado e idéntico, script servido por curl idéntico, `health` 200. Pendiente: confirmación visual en navegador con `?es-debug=1`.
- [x] Fix es-badge v4: la app usa XHR (axios), no window.fetch (2026-08-17): verificado con Chrome headless + sesión real que el wrapper de `window.fetch` no capturaba las respuestas de los sliders → el mapa de pósters quedaba vacío y solo se pintaban insignias en tarjetas "hovereadas". Se añade `interceptXhr()` (envuelve `XMLHttpRequest.prototype.open/send`, parsea `responseText` en `load`) y la cola `WAITING[id]` para pintar los duplicados que llegan con la consulta en vuelo. Verificado de punta a punta en Chrome headless (login por API + `?es-debug=1`): 171 pósters capturados del tráfico real, insignias en 10 tarjetas, `offsetParent` de la barra === la tarjeta, geometría 6px/6px en la esquina inferior-derecha de cada tarjeta, duplicados pintados idénticos en todas sus instancias. Tests de unidad 28/28; ConfigMap y script servido idénticos; `health` 200; pod 2/2 Running. Confirmación visual: chrome headless (screenshot; el modelo no puede leer imágenes, pero offsetParent y geometría lo corroboran).
- [x] Fix es-badge v3: anclaje de la insignia a la tarjeta y pintado en duplicados (2026-08-17): en el HTML real las insignias se pintaban pero no se veían — `div[data-testid="title-card"]` no tiene `position`, así que el `position:absolute` de `.es-badge-bar` se anclaba al slider (las barras de un carrusel se apilaban en su esquina inferior-derecha); además el dedupe por `mediaType:tmdbId` hacía `return` antes de pintar (un mismo título en varios sliders solo recibía insignia en la primera tarjeta procesada). Fix: (1) CSS `[data-testid="title-card"]{position:relative}` para anclar cada barra a su tarjeta; (2) caché `FLAGS[id]` que pinta `badgeCard(card, FLAGS[id])` en duplicados con `DONE[id]` (1 consulta por película, todas sus tarjetas con insignia). Tests de unidad 24/24; ConfigMap `es-badge-js` regenerado e idéntico, script servido idéntico, `health` 200, pod 2/2 Running. Pendiente: confirmación visual en navegador.
- [x] Despliegue y verificación de la landing con la nueva sección (2026-08-16): ConfigMap `landing-html` ~149 KB base64, pods Running, `/acerca-de-ti.js` 200 y md5 idéntico al local
- [x] Despliegue de la landing ciberpunk (2026-08-17): fuentes auto-alojadas (6 woff2, subset latin), ConfigMap ~296 KB base64, pods Running, curl 200 en `/`, `/styles.css` y fuentes con md5 idéntico al local. `deploy-landing.sh` migrado a `kubectl apply --server-side` (la anotación `last-applied` del client-side superaba su tope de 256 KiB con el ConfigMap engordado)
- [x] Documentación actualizada (README-TECH § Web pública, AGENTS.md)
- [x] Indexadores torrent en castellano + definición Cardigann custom DivxTotal (2026-08-19): evaluadas DonTorrent, EliteTorrent y DivxTotal. **Descartadas**: DonTorrent (doble anti-bot: Anubis + PoW por descarga vía `api_validate_pow.php`, inasumible para Cardigann) y EliteTorrent (acortador de descargas muerto `acortame-esto.com` → google.com). **DivxTotal operativo**: definición Cardigann v11 propia (`files/multimedia/prowlarr-defs/divxtotal.yml`) entregada como ConfigMap `prowlarr-custom-defs` montado en `/config/Definitions/Custom` (wizard y deploy aplican ConfigMap + manifiestos; cambios requieren rollout de prowlarr). Grab en 2 saltos (bloque `download.selectors`): GET de la página de detalle → extrae `download_tt.php` → `.torrent`. Purga de los anglófonos 1337x (id 5) y Torrent Downloads (id 8); queda DivxTotal (id 9), Torrent9 (6), LimeTorrents (7). es-badge actualizado a `INDEXER_IDS=6,7,9` (`es-badge.yaml` + default en `es-badge-api.js`), redeploy y verificado (`/search?mediaType=movie&tmdbId=693134` → `{"ES":true,...,"total":40}`). Sync fullSync a Sonarr/Radarr verificado (3 indexadores). Verificación E2E: `/api/v1/search?query=dune` → 15 resultados DivxTotal con categorías correctas (Movies/SD, Movies/HD, TV/SD) y grab `/9/download` → `.torrent` bencoded válido; `TEST_INDEXERS=1` → los 3 OK. Supersede la entrada de indexadores del 2026-08-17. Documentado en 03-Aplicaciones.md
